# из какого имеджа
FROM node:16.14.2
# задается место рабочей дериктории в контейнере
WORKDIR /app
# копирируем наш код в контейнер
COPY . .
# команда перед сборкой самого образа
RUN yarn
# порт на котором можно собрать образ
ENV PORT 8080
# Выброс порта для докера
EXPOSE $PORT
# команда для запуска образа
CMD ["yarn", "start:dev"]