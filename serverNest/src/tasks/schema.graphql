type Task {
  id: ID
  peopleId: String
  message: String
}

type Query {
  getAllTasks: [Task!]!
}

type Mutation {
  addTask(peopleId: String, message: String!): Task
  editTask(id: String!, peopleId: String, message: String!): Task
  removeTask(id: String!): Task
}